--[[
    FlightService.luau
    ────────────────────
    Stub — implementation in TASKS.md Phase 1+.
    Follows the standard service pattern (PRD §1.4).
]]

local FlightService = {}

function FlightService.init(_player: Player)
end

function FlightService.cleanup(_player: Player)
end

return FlightService
