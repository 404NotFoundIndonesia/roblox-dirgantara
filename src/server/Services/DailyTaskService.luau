--[[
    DailyTaskService.luau
    ───────────────────────
    Stub — implementation in TASKS.md Phase 1+.
    Follows the standard service pattern (PRD §1.4).
]]

local DailyTaskService = {}

function DailyTaskService.init(_player: Player)
end

function DailyTaskService.cleanup(_player: Player)
end

return DailyTaskService
