--[[
    DataService.luau
    ──────────────────
    Stub — implementation in TASKS.md Phase 1+.
    Follows the standard service pattern (PRD §1.4).
]]

local DataService = {}

function DataService.init(_player: Player)
end

function DataService.cleanup(_player: Player)
end

return DataService
