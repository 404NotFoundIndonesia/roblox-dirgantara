--[[
    KegelapanService.luau
    ───────────────────────
    Stub — implementation in TASKS.md Phase 1+.
    Follows the standard service pattern (PRD §1.4).
]]

local KegelapanService = {}

function KegelapanService.init(_player: Player)
end

function KegelapanService.cleanup(_player: Player)
end

return KegelapanService
